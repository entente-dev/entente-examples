{
  "name": "castle-graphql",
  "version": "0.1.0",
  "description": "Example GraphQL provider service for French rulers - demonstrating Entente GraphQL contract testing",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "build:worker": "tsc --project tsconfig.worker.json",
    "build:all": "pnpm run build && pnpm run build:worker",
    "dev": "wrangler dev",
    "dev:node": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "test": "vitest run test/provider.test.ts",
    "deploy": "wrangler deploy",
    "deploy:dev": "wrangler deploy --env development",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:prod": "wrangler deploy --env production",
    "register:service": "entente register-service --spec spec/schema.graphql",
    "upload:spec": "entente upload-spec --spec spec/schema.graphql",
    "deploy:service": "entente deploy-service --environment development",
    "can-i-deploy": "entente can-i-deploy --environment development",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@entente/fixtures": "^0.1.17",
    "@entente/provider": "^0.1.17",
    "@entente/types": "^0.1.17",
    "@hono/node-server": "^1.13.5",
    "dotenv": "^17.2.2",
    "graphql": "^16.9.0",
    "graphql-yoga": "^5.8.0",
    "hono": "^4.6.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250913.0",
    "@entente/cli": "^0.1.17",
    "@types/node": "^24.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "wrangler": "latest"
  },
  "keywords": [
    "entente",
    "contract-testing",
    "example",
    "provider",
    "graphql",
    "rulers"
  ]
}
